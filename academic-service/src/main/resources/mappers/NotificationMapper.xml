<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.academic.application.notification.NotificationMapper">

    <!-- NOTI-01 알림 목록 조회 -->
    <select id="findNotifications">
        SELECT
        n.noti_id,
        n.ref_id,
        n.url,
        n.message,
        n.is_read,
        n.created_at,
        n.type,
        COUNT(*) OVER() AS total_count
        FROM notification n
        WHERE n.member_code = #{memberCode}
        <if test="req.isRead != null">
            AND n.is_read = #{req.isRead}
        </if>
        ORDER BY n.created_at DESC
        LIMIT #{req.size} OFFSET #{req.startIdx}
    </select>

</mapper>